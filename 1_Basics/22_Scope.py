def greet():
    if True :
        message = "a"
    print(message)


#in this section he comaperd the use of global variable and locla variable
#it's better to avoid global variable since it can affect the other variables
